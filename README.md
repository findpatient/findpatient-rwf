# findpatient-rwf

This repository contains the Royal Wolverhampton NHS Trust (RWF) configuration and deployment workflow for the FindPatient app.

- No application code lives here.
- All code is pulled from the core repository during deployment.
- This repo contains:
  - org.json (branding + metadata)
  - GitHub Secrets (URLs + status flags)
  - GitHub Actions workflow (deployment)
